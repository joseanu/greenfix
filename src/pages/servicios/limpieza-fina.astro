---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import { Image } from 'astro:assets';
import img from '../../assets/servicioshome2.jpg';

const title = "Servicio de Limpieza Fina";
const description = "Atención al detalle para residencias de lujo y espacios corporativos premium. Limpieza meticulosa en Guadalajara.";

const items = [
  { title: "Detalle Extremo", desc: "Limpieza minuciosa de molduras, lámparas y accesorios delicados." },
  { title: "Cuidado de Superficies", desc: "Tratamiento especial para maderas, granitos y acabados de lujo." },
  { title: "Ambientes Saludables", desc: "Eliminación de alérgenos y polvo fino con microfibra." },
  { title: "Organización", desc: "Cuidado en el acomodo y preservación del orden en el espacio." },
];
---

<Layout title={title} description={description} bodyClass="siteServicioDetalle">
  <Header />
  <main>
    <header class="service-hero">
      <div class="container">
        <h1>{title}</h1>
        <p>Excelencia y discreción para los espacios más exigentes.</p>
      </div>
    </header>
    <div class="container section-padding">
      <div class="row row-center">
        <div class="column column-50">
          <div class="service-content">
            <h2>El Arte del Detalle</h2>
            <p>La limpieza fina va más allá del aseo convencional. Se trata de preservar la belleza de sus acabados y asegurar que cada elemento de su hogar u oficina luzca como nuevo. En Green Fix, nuestro personal está entrenado para manejar materiales de alta gama con el mayor cuidado.</p>
            <p>Garantizamos un servicio discreto, puntual y con resultados que se ven y se sienten.</p>
          </div>
        </div>
        <div class="column column-50">
          <Image src={img} alt="Limpieza fina residencial" class="img-rounded shadow" />
        </div>
      </div>
      <div class="tasks-grid">
        {items.map(item => (
          <div class="task-card">
            <h3>{item.title}</h3>
            <p>{item.desc}</p>
          </div>
        ))}
      </div>
      <div class="service-cta">
        <div class="cta-box text-center">
          <h3>¿Busca un servicio de confianza?</h3>
          <p>Contacte con nosotros para una cotización personalizada de limpieza fina.</p>
          <a href="/contacto" class="button button-primary">Agendar Cita</a>
        </div>
      </div>
    </div>
  </main>
</Layout>

<style lang="scss">
  @use "../../styles/parcial/variables" as *;
  .service-hero { background: $brand-color; padding: 18rem 0 10rem; color: white; text-align: center; h1 { color: white; font-size: 4rem; margin-bottom: 2rem; } p { font-size: 2rem; opacity: 0.9; max-width: 800px; margin: 0 auto; } }
  .section-padding { padding: 10rem 0; }
  .service-content { padding-right: 5rem; h2 { font-size: 3.2rem; margin-bottom: 2.5rem; } p { font-size: 1.8rem; line-height: 1.8; color: $text-light; margin-bottom: 2rem; } }
  .tasks-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(30rem, 1fr)); gap: 3rem; margin-top: 8rem; }
  .task-card { background: $section-bg; padding: 3rem; border-radius: 8px; border-left: 4px solid $brand-color; h3 { font-size: 2rem; margin-bottom: 1rem; color: $negro; } p { font-size: 1.5rem; color: $text-light; margin-bottom: 0; } }
  .service-cta { margin-top: 10rem; .cta-box { background: white; padding: 6rem; border-radius: 12px; box-shadow: 0 20px 60px rgba(0,0,0,0.08); h3 { font-size: 2.8rem; margin-bottom: 1.5rem; } p { font-size: 1.8rem; color: $text-light; margin-bottom: 3.5rem; } } }
  @media (max-width: 40rem) { .service-hero { padding: 15rem 0 8rem; h1 { font-size: 3rem; } } .service-content { padding-right: 0; margin-bottom: 4rem; } .cta-box { padding: 3rem; } }
</style>
