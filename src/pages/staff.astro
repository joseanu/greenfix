---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import { Image } from 'astro:assets';
import staffImg from '../assets/staff.jpg';

const title = "Nuestro Equipo - Staff Green Fix";
const description = "Conoce al equipo de profesionales detrás de los servicios de limpieza y mantenimiento de Green Fix.";

const team = [
  { name: "Equipo Operativo", role: "Técnicos Especialistas", image: staffImg },
  { name: "Supervisión", role: "Control de Calidad", image: staffImg },
  { name: "Administración", role: "Atención al Cliente", image: staffImg },
];
---

<Layout title={title} description={description} bodyClass="siteStaff">
  <Header />
  
  <main class="container section-padding">
    <div class="services-intro">
      <h1>Nuestro <span>Equipo</span></h1>
      <p>Contamos con técnicos especializados y personal administrativo comprometido con la excelencia en el servicio.</p>
    </div>

    <div class="team-grid">
      {team.map(member => (
        <div class="team-card">
          <div class="team-image">
            <Image src={member.image} alt={member.name} />
          </div>
          <h3>{member.name}</h3>
          <span>{member.role}</span>
        </div>
      ))}
    </div>

    <section class="section-padding" style="margin-top: 8rem; text-align: center; background: #f8f9fa; border-radius: 12px;">
       <div style="max-width: 800px; margin: 0 auto; padding: 0 2rem;">
         <h2>Capacitación Constante</h2>
         <p>En Green Fix, el personal es nuestro activo más valioso. Por ello, mantenemos programas de capacitación continua en técnicas de limpieza profunda, manejo de químicos certificados y seguridad industrial.</p>
       </div>
    </section>
  </main>
</Layout>

<style lang="scss">
  @use "../styles/parcial/variables" as *;
  .section-padding { padding: 12rem 0 8rem; }
  h1 span { color: $brand-color; }
</style>